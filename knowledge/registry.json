{
  "_superseded": {
    "date": "2026-09-23",
    "status": "SUPERSEDED — do not read this file for facts about the system",
    "was": "Hand-written manifest v1.0.0 (updated 2026-09-09): servers[], files[], services{}, endpoints{}, gaps[].",
    "why": [
      "Read by no code. grep for 'registry.json' finds only prose references (AGENTS.md, docs/refactor-plan.md, hub/guides/deployment.md, knowledge/decisions.sql) — no loader, no handler, no script.",
      "files[] described hub/server.py as a 3,119-line monolith of type 'monolith'. It is 152 lines of bootstrap. Phases 1 and 2 shipped; this file was never updated.",
      "services{} and endpoints{} were hand-typed copies of state the hub derives live. hub/CONSTITUTION.md section 4: a BOM may never be prose.",
      "servers[] duplicated knowledge/servers.json, which is still current and still holds both machine UUIDs."
    ],
    "replaced_by": {
      "what this node is (the BOM)": "GET /api/receipt",
      "services + live docker state": "GET /api/services",
      "ports actually listening": "GET /api/ports",
      "every route that exists": "GET /api/sitemap",
      "code inventory / telescope codes": "python hub/tools/registry_gen.py -> knowledge/registry-live.json (generated, never hand-edited)",
      "servers and UUIDs": "knowledge/servers.json",
      "architecture decisions": "knowledge/decisions.sql",
      "roadmap and what is not built": "TASKS.md, AGENTS.md"
    },
    "kept_below": "gaps[] only — open work items with their intended fix. These are knowledge, not a BOM, and are not derivable from any endpoint. Everything else was dropped."
  },
  "gaps": [
    {
      "id": "G001",
      "item": "Server UUIDs populated via /etc/machine-id",
      "file": "knowledge/servers.json",
      "status": "resolved",
      "note": "dmidecode returned Not Settable on fks-services; machine-id used instead"
    },
    {
      "id": "G002",
      "item": "fix-netplan.sh not in install.sh",
      "file": "server-kit/install.sh",
      "fix": "Add call to fix-netplan.sh in install.sh"
    },
    {
      "id": "G003",
      "item": "boot-health-check not on fks-services",
      "fix": "Deploy server-kit/tools/boot-health-check.sh to fks-services"
    },
    {
      "id": "G004",
      "item": "/api/hub-context not built",
      "fix": "Phase 1 — add after kernel extraction"
    },
    {
      "id": "G005",
      "item": "Backup systemd timer not set",
      "fix": "server-backup.sh exists, needs timer unit"
    },
    {
      "id": "G006",
      "item": "Nextcloud not behind NPM",
      "fix": "Need NPM admin password to add proxy host"
    },
    {
      "id": "G007",
      "item": "Auth gate missing on Receipt + AI Chat views",
      "fix": "Phase 2/3 — add gate_check(2) to those handlers"
    },
    {
      "id": "G008",
      "item": "TOTP gate not enforced on hub",
      "fix": "Phase 1 — router-level auth enforcement"
    },
    {
      "id": "G009",
      "item": "FV MCP server not built",
      "fix": "Phase 4"
    },
    {
      "id": "G010",
      "item": "Ubuntu autoinstall ISO generator not built",
      "fix": "Phase 4 / FV milestone"
    }
  ]
}
