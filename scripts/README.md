# Scripts

Drop shell scripts here for common server tasks.
Claude can read and run these via SSH.

## Naming Convention
- `check-*.sh` — status/health checks
- `start-*.sh` — start a stack or service
- `stop-*.sh` — stop a stack or service
- `fix-*.sh` — one-off fixes for known issues
