/* LookupWidget — STUB.
 *
 * The real one lives in the app this shell was taken from and is that app's
 * content, not shell. It is stubbed rather than deleted because WidgetBar
 * imports it by name: removing the file breaks the build, and an empty
 * template that does not build is not a template.
 *
 * Replace the body with a ServerHub widget. The contract is only that it
 * renders something inside the widget dock.
 */
export default function LookupWidget() {
  return (
    <div className="p-3 text-xs text-muted-foreground">
      LookupWidget — not implemented
    </div>
  )
}
